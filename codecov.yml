coverage:
  status:
    project:
      default:
        target: auto
        threshold: 5%
        informational: true
    patch:
      default:
        target: 0%
        informational: true

comment:
  layout: "diff, files"
  behavior: default
  require_changes: true
